{
    "name": "cost-analyzer-lambda",
    "version": "1.0.0",
    "description": "AWS Cost Analyzer Lambda Function",
    "main": "index.js",
    "scripts": {
        "clean": "rimraf dist",
        "build": "npm run clean && esbuild index.ts --bundle --platform=node --target=node18 --outfile=dist/index.js --external:@aws-sdk/*",
        "prepack": "npm run build"
    },
    "dependencies": {
        "@aws-sdk/client-account": "^3.980.0",
        "@aws-sdk/client-cloudwatch": "^3.974.0",
        "@aws-sdk/client-cost-explorer": "^3.974.0",
        "@aws-sdk/client-s3": "^3.974.0",
        "@aws-sdk/client-ses": "^3.974.0",
        "rimraf": "^5.0.10"
    },
    "devDependencies": {
        "@types/aws-lambda": "^8.10.130",
        "@types/node": "^22.0.0",
        "esbuild": "^0.27.2",
        "typescript": "^5.9.3"
    }
}
