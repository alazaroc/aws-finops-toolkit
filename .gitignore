# Dependencies
node_modules/
yarn.lock

# Build outputs
dist/
build/

# Per-lambda build outputs (keep source tree clean)
src/**/dist/

# Avoid ignoring all generated files globally (e.g. "*.js" or "*.d.ts").
# Prefer to ignore build directories (dist/, .aws-sam/) and package outputs.
# If you need to track a specific generated file, add an explicit exception or
# change the build output location to `dist/` so artifacts are clearly separate.
!jest.config.js
!.eslintrc.js
response-*.json

# AWS SAM
.aws-sam/

# Test coverage
coverage/
.nyc_output/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local
.env.*.local

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Temporary files
tmp/
temp/
*.tmp
todo/

# project generated files
samconfig.generated.toml
template.generated.yaml
response-*.json

# Local backups
*.bundle

# Configuration (keep secrets out of git)
config/config.yml
